<project name="nChat" basedir="." default="jar">

	<property name="src.dir" value="src"/>
	<property name="build.dir" value="build"/>
	<property name="classes.dir" value="${build.dir}/classes"/>
	<property name="jar.dir" value="${build.dir}/jar"/>

	<path id="classpath">
		<fileset dir="${env.WORKSPACE}../../../../Framework/workspace/build/jar/" includes="framework.jar"/>
		<fileset dir="${env.WORKSPACE}../../../../Framework/workspace/lib/" includes="**/*.jar"/>
	</path>

	<target name="clean">
		<delete dir="${build.dir}"/>
	</target>

	<target name="compile">
		<mkdir dir="${classes.dir}"/>
		<javac srcdir="${src.dir}" destdir="${classes.dir}" classpathref="classpath"/>
	</target>

	<target name="jar" depends="compile">
		<mkdir dir="${jar.dir}"/>
		<jar destfile="${jar.dir}/nChat.jar" basedir="${classes.dir}">
			<manifest>
				<attribute name="Class-Path" value="runsafe/framework.jar" />
			</manifest>
			<fileset dir="${src.dir}" includes="plugin.yml"/>
			<fileset dir="${src.dir}" includes="default.yml"/>
		</jar>
	</target>

	<target name="clean-build" depends="clean,jar"/>

</project>